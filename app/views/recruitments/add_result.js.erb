$('#add_results').append(
    "<%= escape_javascript(render partial: :comment, collection: @comments, form: f) %>"
);
$('#request_add_results').html(
    "<%= escape_javascript(button_to "さらに読み込む", comments_add_path(params[:id],  (params[:size].to_i+@comments.size).to_s), method: :get, remote: true) %>"
);