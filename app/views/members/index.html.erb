<p id="notice"><%= notice %></p>
<table>
  <thead>
  <tr>
    <th>ID/</th>
    <th>チャットID/</th>
    <th>アカウントID/</th>
    <th colspan="3"></th>
  </tr>
  </thead>

  <tbody>
  <% @chat_members.each do |entry_chat| %>
    <tr>
      <td><%= entry_chat.id %></td>
      <td><%= entry_chat.chat_id %></td>
      <td><%= entry_chat.acc_id %></td>
    </tr>
  <% end %>
  </tbody>
</table>
<table class="button">
  <%= button_to 'チャットに戻る', chat_comments_path, method: :get %>
</table>
