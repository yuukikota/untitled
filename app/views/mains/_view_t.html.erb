<%= stylesheet_link_tag "box.css", :media => "all" %>
<span class="view">

<table>

  <% recruitments.each do |recruitment| %>
    <div class="box_comment">

      <%= link_to account_show_path(recruitment.acc_id) do %>
        <span class="box_comment2">
          <%= Account.find_by(acc_id: recruitment.acc_id).name %>👤
        </span>
      <% end %>
      <span class="box_comment2"><%= recruitment.acc_id %></span>
      <span class="box_comment2"><%= recruitment.created_at %></span>
      <br>
      <% if recruitment.re_id == '募集'%>
        <div class="box-title"><%= recruitment.title %></div>
        <span class="box_comment2">(チャット<%= recruitment.chat %>)</span>
      <% end %>
      <div class="box_comment3">
        <%= simple_format(recruitment.detail) %>
      </div>
      <% if recruitment.resolved == '解決' %>
      <div class="box_comment4">
        <div class="box-title">回答🔍</div>
        <%= simple_format(recruitment.answer) %>
      </div>
        <% end %>
      <span class="box_comment2">
        <%= link_to '返信📨', comments_index_path(recruitment.id), method: :get  %>
      </span>
      <span>
        <%= render partial: 'bookmarks/bookmark', locals: { recruitment_id: recruitment.id} %>
      </span>
      <span>
        <% if account_signed_in? %>
          <% if current_account.acc_id == recruitment.acc_id %>
            <% if recruitment.re_id == '募集'%>
              <% if  recruitment.resolved == '未解決' %>
                <% if recruitment.chat == "有" %>
                  <span class="box_comment2"><%= link_to '募集結果選択☝', new_entry_chats_path(recruitment.id), method: :get  %></span>
                <% elsif recruitment.chat == "無" %>
                  <span class="box_comment2"><%= link_to '募集結果選択☝', edit_recruitment_path(recruitment.id), method: :get  %></span>
                <% end %>
              <% elsif recruitment.resolved == '解決' && recruitment.chat == "有" %>
                <span>
                  <span class="box_comment2"><%= link_to 'チャット💭', chat_comments_index_path(recruitment.id), method: :get  %></span>
                </span>

              <% end %>
            <%end %>
            <span class="box_comment2"><%= link_to '削除🚮', recruitment, method: :delete, data: { confirm: '削除しますか?' } %></span>
          <% elsif current_account.acc_id == 'administrator' %>
            <% if recruitment.resolved == '解決' && recruitment.chat == "有" %>
                  <span class="box_comment2"><%= link_to 'チャット💭', chat_comments_index_path(recruitment.id), method: :get  %></span>
            <% end %>
            <span class="box_comment2"><%= link_to '削除🚮', recruitment, method: :delete, data: { confirm: '削除しますか?' } %></span>
          <% elsif  EntryChat.where(chat: recruitment.id).find(acc_id: current_account.acc_id).present? %>
              <span class="box_comment2"><%= link_to 'チャット💭', chat_comments_index_path(recruitment.id), method: :get  %></span>
          <%end %>
        <% end %>
        </span>
      <br>
      <br>
    </div>
  <% end %>
</table>
</span>